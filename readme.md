Builds an Arch Linux live image with ZFS.  Script needs to be executed
under Arch.

Run `./build.sh`. Or `use_dkms=1 ./build.sh` to use DKMS build
of ZFS, which is useful if archzfs is out of sync with the kernel
package.
